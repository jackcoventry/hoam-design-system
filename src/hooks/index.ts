/** Trap keyboard focus within a container while active. */
export { useFocusTrap } from '@/hooks/useFocusTrap';
/** Keyboard navigation helper for composite menu and navigation patterns. */
export { useKeyboardNav } from '@/hooks/useKeyboardNav';
/** React hook for subscribing to CSS media query matches. */
export { useMediaQuery } from '@/hooks/useMediaQuery';
/** State machine for responsive mega-navigation interactions. */
export { useMegaNavState } from '@/hooks/useNavState';
/** React hook for the user's `prefers-reduced-motion` preference. */
export { usePrefersReducedMotion } from '@/hooks/usePrefersReducedMotion';
