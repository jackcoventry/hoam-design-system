/** Props for the shared button and link-button component. */
export type { ButtonProps, LinkComponentProps } from './Button';
/** Primary action component for buttons and navigational links. */
export { Button, ButtonVariants } from './Button';
