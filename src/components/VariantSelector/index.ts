/** Types for product variant selection. */
export type {
  VariantOption,
  VariantSelectorProps,
  VariantValue,
} from '@/components/VariantSelector/VariantSelector';
/** Accessible selector for product variants such as size, roast, or finish. */
export { VariantSelector } from '@/components/VariantSelector/VariantSelector';
