/** Props for the hero carousel and individual hero slides. */
export type { HeroProps } from '@/components/Hero/Hero';
/** Marketing hero that renders one or more slides with media and calls to action. */
export { Hero } from '@/components/Hero/Hero';
/** Props for an individual hero slide. */
export type { HeroSlideProps } from '@/components/Hero/HeroSlide';
/** Single hero slide with media, copy, and optional actions. */
export { HeroSlide } from '@/components/Hero/HeroSlide';
