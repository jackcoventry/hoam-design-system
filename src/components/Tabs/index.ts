/** Desktop-only tabs renderer. */
export { DesktopTabs } from '@/components/Tabs/Desktop';
/** Props for tab items and the tabs container. */
export type { TabProps, TabsProps } from '@/components/Tabs/Tabs';
/** Tabbed content component with mobile and desktop layout support. */
export { Tabs } from '@/components/Tabs/Tabs';
